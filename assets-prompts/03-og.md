# 03 — OG / social card

## og-card.png

UPLOAD: `public/brand/qahwa-blk-black.webp` (the brand's EXACT logo) — the
model must copy it, never invent it.

```
Social preview photograph, landscape 1200x630. A white paper coffee cup
stands centered on a black stone surface, faint steam rising, one hard
warm side light, deep black background with soft falloff. The attached
image is the brand's exact logo: apply it exactly as it is onto the
front of the cup, facing the camera, sharp and undistorted, like a
printed cup — do not redraw, redesign, translate or reinterpret it;
copy its glyphs and proportions precisely. No other text anywhere in
the image, no watermark. Warm neutral grade, premium and minimal.
```

→ zoom into the mark: if قهوة is warped or invented, regenerate once;
still wrong → generate with "a small blank black square outline" instead
and stamp the real logo over the square in post (ffmpeg/PIL overlay —
the real files are `public/brand/qahwa-blk-black.webp` and
`qahwa-blk-white.webp`).

→ save as `public/og.jpg`, then point the `og:image` meta at `/og.jpg`.
