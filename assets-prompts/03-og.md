# 03 — OG / social card (GPT)

## og-card.png

UPLOAD: `public/brand/qahwa-blk-white.webp` (the real mark goes ON the image
by hand afterwards if the tool distorts it — never let AI redraw the logo)

```
Social preview photograph, landscape 1200x630. A white paper coffee cup
with a small black square outline stands centered on a black stone surface,
one hard warm side light, faint steam, deep black background with soft
falloff. Room at the top third for a logo overlay. Warm neutral grade,
premium and minimal. No readable text anywhere, no logos, no watermark.
```

→ overlay the real white mark top-center at ~140px tall, save as
`public/og.jpg`, then point the `og:image` meta at `/og.jpg`.
